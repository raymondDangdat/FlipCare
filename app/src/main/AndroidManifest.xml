<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comq.example.raymond.flipcare">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreenActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".ServicesActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ChatHistoryActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AboutActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ContactActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ChatActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DoctorSignIn"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DoctorsHome"
            android:label="@string/title_activity_doctors_home"
            android:theme="@style/AppTheme" />
        <activity android:name=".AuthenticationActivity" />
        <activity android:name=".PatientsChatActivity" />
        <activity android:name=".DoctorChatsActivity"></activity>
    </application>

</manifest>